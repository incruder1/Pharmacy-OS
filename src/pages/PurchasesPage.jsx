import { PurchasesPage as PurchasesScreen } from '@/modules/purchases';

/** Thin route wrapper — logic lives in the purchases module. */
export default function PurchasesPage() {
  return <PurchasesScreen />;
}
