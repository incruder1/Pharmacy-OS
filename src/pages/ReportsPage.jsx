import { ReportsPage as ReportsScreen } from '@/modules/reports';

/** Thin route wrapper — logic lives in the reports module. */
export default function ReportsPage() {
  return <ReportsScreen />;
}
