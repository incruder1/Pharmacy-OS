import { SettingsPage as SettingsScreen } from '@/modules/settings';

/** Thin route wrapper — logic lives in the settings module. */
export default function SettingsPage() {
  return <SettingsScreen />;
}
