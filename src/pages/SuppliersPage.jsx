import { SuppliersPage as SuppliersScreen } from '@/modules/suppliers';

/** Thin route wrapper — logic lives in the suppliers module. */
export default function SuppliersPage() {
  return <SuppliersScreen />;
}
