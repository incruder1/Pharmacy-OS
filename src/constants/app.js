export const APP_NAME = 'PharmacyOS';
export const APP_TAGLINE = 'Modern pharmacy management for Indian medical stores';

export const CURRENCY = {
  code: 'INR',
  symbol: '₹',
  locale: 'en-IN',
};

export const DATE_FORMAT = 'DD MMM YYYY';
export const DATE_TIME_FORMAT = 'DD MMM YYYY, hh:mm A';

/** Default simulated network latency for the mock API layer (ms). */
export const MOCK_LATENCY_MS = 450;
