import { PageHeader } from '@/components/PageHeader';

export function ExpiryHeader() {
  return (
    <PageHeader
      title="Expiry Management Center"
      subtitle="Protect margins — track expired stock and batches nearing expiry before they become write-offs"
    />
  );
}

export default ExpiryHeader;
