import { PageHeader } from '@/components/PageHeader';

export function ReorderHeader() {
  return (
    <PageHeader
      title="Smart Reorder Center"
      subtitle="Prevent stockouts before they cost you sales — AI-ready reorder suggestions for your pharmacy"
    />
  );
}

export default ReorderHeader;
